#include <stdio.h>

int main () {
	int num1, num2; // declares 2 variables of int data-type
	char letter; // declares a variable of char data-type
	float decimal = 7.5; // declares and initializes a variable of float data-type and assigns value to it
	num1 = 100; // initializes num1 variable with an integer value
  letter = 'A'; // initializes letter variable with a character value

	printf("%d\n", num1);
	 return 0;
}